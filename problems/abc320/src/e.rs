use std::collections::BTreeSet;

use proconio::{input, marker::Chars, input_interactive};

pub fn main() {
    input! {
        n: usize,
        m: usize,
        twsm: [(i64, i64, i64); m]
    };
    
}