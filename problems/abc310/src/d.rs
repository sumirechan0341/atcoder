use std::collections::{HashMap, HashSet};

use proconio::{input, marker::Chars};
type VS = Vec<String>;

pub fn main() {
    input!{
        n: usize,
        t: usize,
        m: usize,
        abm: [(usize, usize); m]
    };
    
}